[app]
name = "onionshare"
version = "0.1.0"
description = "Anonymously share files, host websites, and chat over Tor hidden services"

[runtime.container]
image = "Dockerfile"
port = 8080

[resources]
memory_mb = 1024
cpu_cores = 2.0

[data]
app_data = true
app_temp_data = true
